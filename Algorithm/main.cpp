#include <iostream>
#include "../Algorithm/Algorithm.h"

int main() {
  // MultiMap<double> map(100);
  // map.add(1, 10.2);
  // map.add(2, 10.5);
  // map.add(1, 20.2);
  // map.remove(2);
  // map.add(2, 40.1);

  // auto f1 = map.find(1);
  // auto f2 = map.find(2);

  // for (int i = 0; i < f1->data_num; i++) {
  //   cout << f1->data[i] << endl;
  // }

  // for (int i = 0; i < f2->data_num; i++) {
  //   cout << f2->data[i] << endl;
  // }
  // int a[10] = {15, 3, 5, 700, 3, 6, 7, 8, 9, 10};
  // quickSort(a, 0, 10);
  // for (auto i : a) {
  //   cout << i << endl;
  // }
  // cout << "--------------------------\n";
  // int data_num = removeDuplicate(a, 10);
  // for (int i = 0; i < data_num; i++) {
  //   cout << a[i] << endl;
  // }

  int a = 10;
  std::cout << max(15, 25, 35, 45) << std::endl;
}